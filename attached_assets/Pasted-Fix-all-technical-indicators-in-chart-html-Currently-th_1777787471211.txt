Fix all technical indicators in chart.html. 
Currently they show empty/broken. 

Implement these indicators fully using the 
OHLCV data already fetched from Yahoo Finance:

MOVING AVERAGES (already on chart, fix them):
Calculate from close prices array:
function SMA(closes, period) {
  return closes.map((_, i) => {
    if (i < period - 1) return null;
    const slice = closes.slice(i - period + 1, i + 1);
    return slice.reduce((a, b) => a + b) / period;
  });
}

function EMA(closes, period) {
  const k = 2 / (period + 1);
  const ema = [closes[0]];
  for (let i = 1; i < closes.length; i++) {
    ema.push(closes[i] * k + ema[i-1] * (1 - k));
  }
  return ema;
}

Add these indicator overlays on the main chart:
- MA 20 (yellow line)
- MA 50 (blue line)  
- MA 200 (red line)
- EMA 9 (white line, thin)

Add these as separate panes below the chart:

VOLUME PANE (always shown):
- Green bars when close > open
- Red bars when close < open
- Already showing, make sure it works with real data

RSI PANE (toggleable):
function RSI(closes, period = 14) {
  const gains = [], losses = [];
  for (let i = 1; i < closes.length; i++) {
    const diff = closes[i] - closes[i-1];
    gains.push(diff > 0 ? diff : 0);
    losses.push(diff < 0 ? Math.abs(diff) : 0);
  }
  const rsi = [];
  let avgGain = gains.slice(0,period).reduce((a,b)=>a+b)/period;
  let avgLoss = losses.slice(0,period).reduce((a,b)=>a+b)/period;
  for (let i = period; i < gains.length; i++) {
    avgGain = (avgGain * 13 + gains[i]) / 14;
    avgLoss = (avgLoss * 13 + losses[i]) / 14;
    const rs = avgGain / avgLoss;
    rsi.push(100 - 100/(1+rs));
  }
  return rsi;
}
- Show RSI line in gold color
- Draw horizontal lines at 70 (overbought) and 30 (oversold)
- Color fill: red zone above 70, green zone below 30

MACD PANE (toggleable):
function MACD(closes) {
  const ema12 = EMA(closes, 12);
  const ema26 = EMA(closes, 26);
  const macdLine = ema12.map((v, i) => v - ema26[i]);
  const signalLine = EMA(macdLine.filter(v => v !== null), 9);
  const histogram = macdLine.slice(26-1).map((v, i) => v - signalLine[i]);
  return { macdLine, signalLine, histogram };
}
- MACD line: blue
- Signal line: orange  
- Histogram: green if positive, red if negative

BOLLINGER BANDS (overlay on main chart, toggleable):
function BollingerBands(closes, period=20, mult=2) {
  const sma = SMA(closes, period);
  return closes.map((_, i) => {
    if (i < period-1) return null;
    const slice = closes.slice(i-period+1, i+1);
    const mean = sma[i];
    const std = Math.sqrt(slice.reduce((a,b) => a + Math.pow(b-mean,2), 0) / period);
    return { upper: mean + mult*std, middle: mean, lower: mean - mult*std };
  });
}
- Upper band: dashed gold line
- Middle band: solid grey line
- Lower band: dashed gold line
- Fill between bands: very subtle gold rgba(201,160,48,0.05)

INDICATOR TOGGLE BUTTONS:
Add toggle buttons in the toolbar:
[MA] [EMA] [BB] [RSI] [MACD] [VOL]
- Active state: gold background
- Inactive: outline only
- Clicking toggles the indicator on/off

When indicator data is calculated, map it to 
TradingView Lightweight Charts LineSeries format:
const rsiData = rsiValues.map((v, i) => ({
  time: timestamps[i + 14],
  value: v
}));
rsiSeries.setData(rsiData);

All calculations done in JavaScript from the 
OHLCV data fetched from Yahoo Finance.
No external indicator libraries needed.
Match the existing chart.html dark theme.